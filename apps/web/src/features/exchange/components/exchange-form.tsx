import React from 'react';

import { Button } from "@/components/ui/button";

export const ExchangeForm = () => {
  return (
    <div>
      <Button>
        Przelicz
      </Button>
    </div>
  );
};
